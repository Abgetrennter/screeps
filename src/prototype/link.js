/*Object.defineProperty(StructureLink.prototype, 'isin', {
    get: function () {
        if (!this._in) {

            this._in = (this.pos.findInRange(FIND_SOURCES,4));
        }
        return this._Full;
    },
    enumerable: false,
    configurable: true
});*/